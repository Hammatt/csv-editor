# CSV Editor

A toy app to edit CSVs

## Running the app

`npm start`

## Deploying the app

1. Run `npm run build`
2. Upload the bundle to S3 or something.

## TODO
- Edit the CSV
- Export the CSV
- Tests
